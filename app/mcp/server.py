from fastapi import FastAPI


def build_mcp_server():
    """MCP adapter placeholder; tools must call Z1 service-layer APIs."""
    return {"name": "z1-mcp", "status": "scaffold", "direct_db_access": False}
